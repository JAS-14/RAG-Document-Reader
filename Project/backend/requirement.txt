# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20   # required by FastAPI for File/UploadFile handling
pydantic==2.10.4

# --- Database (MySQL via SQLAlchemy) ---
sqlalchemy==2.0.36
pymysql==1.1.1

# --- Auth (JWT + password hashing) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- Environment variables ---
python-dotenv==1.0.1

# --- RAG / LLM stack ---
openai==1.58.1
langchain==0.3.13
langchain-openai==0.2.14
langchain-community==0.3.13
langchain-text-splitters==0.3.4
pypdf==5.1.0                # required by PyPDFLoader
pinecone==5.4.2              # note: package renamed from pinecone-client -> pinecone

# --- Hybrid search / reranking / evaluation ---
rank-bm25==0.2.2             # local BM25 sparse retrieval, fused with dense via RRF
sentence-transformers==3.3.1 # local cross-encoder reranker
ragas==0.2.10                # RAG evaluation harness (faithfulness, relevancy, etc.)
datasets==3.2.0              # required by ragas